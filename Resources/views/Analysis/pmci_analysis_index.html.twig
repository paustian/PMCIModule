{% form_theme form 'ZikulaFormExtensionBundle:Form:bootstrap_3_zikula_admin_layout.html.twig' %}
{{ moduleLinks() }}
<div class="container-fluid">
    <div class="row">
        <h3>
            <span class="fa fa-edit"></span>
            {{ __("Analyze your MCI data") }}
        </h3>
        <p>{{ __('Use this interface to compare responses between two surveys, or subclasses of surveys.') }}</p>
        {{ showflashes() }}
        {{ form_start(form) }}
        {{ form_errors(form) }}
        <fieldset>
            <legend>{{ __('Analyze MCI Surveys') }}</legend>

        </fieldset>
        <div class="form-group">
            <div class="col-lg-offset-3 col-lg-9">

            </div>
        </div>
        {{ form_end(form) }}
    </div>
</div>